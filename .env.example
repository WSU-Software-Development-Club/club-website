# Neon Postgres connection string, used by server.ts (local dev) and api/*.ts (Vercel functions).
DATABASE_URL=

# Admin password used for logging into the admin panel
ADMIN_PASSWORD=

# Admin session secret used to sign and verify admin session tokens.
ADMIN_SESSION_SECRET=